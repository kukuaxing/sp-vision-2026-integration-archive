# 新对话交接：XUC V2 实车自瞄联调

更新时间：2026-09-03（Asia/Shanghai）

> 新对话请先完整阅读本文，再查看各子目录 README。不要根据阶段名称猜测实车状态；以本文“已烧录/未烧录”标记和 SHA-256 为准。

## 1. 最终目标与当前结论

目标是让小电脑识别红色装甲板后，控制 yaw 云台跟随，使装甲板稳定收敛到画面中心；射击暂不在本轮范围内。

当前已经打通：

- 海康相机取流和红色装甲板识别。
- 小电脑经 CH340 与下位机 USART6 的 460800 波特率双向 XUC V2 通信。
- 下位机 IMU 的 yaw/pitch 上报、CRC、丢包/超时保护和上位机诊断。
- 下位机接收 control/yaw/pitch 指令并能实际驱动 yaw 云台。
- DR16 模式切换能够让下位机上报 `mode=1`，上位机进入 `auto_aim`。

尚未完成：

- yaw 自动跟随的最终收敛验证。Phase 2D.3 曾证明正确 AUTO 符号能够收敛，但存在约 3.4° 静摩擦残差；Phase 2D.4 改错符号后会发散。
- Phase 2D.5 已针对上述问题完成代码和构建，尚未烧录、尚未实车验证。
- pitch 云台当时机械锁死，本轮没有验证 pitch 跟随。
- 摩擦轮、拨弹和实弹闭环全部未启用。

## 2. 绝不能混淆的版本状态

### 当前机器人内的固件：Phase 2D.4

- 提交：`331f080c9dd0d213e513a5c0d412421aca2ebae8`
- bin SHA-256：`cf13b6e7ea65106bd1f43ba8be39daf64236a4e5e9e90e3df88454fd5151173d`
- 已执行 OpenOCD program/verify，并做过同长度 Flash 回读比对。
- 实车结论：通信正常、云台能动，但 AUTO 符号错误，会把装甲板推向画面边缘。**不要继续用 D4 做跟随调参。**

### 下一待测固件：Phase 2D.5

- 固件代码提交：`4fb76dbba388e9fb09e2dd8b8b8ed1d253d92105`
- 含交接文档的源码快照 HEAD：`5ab5e2af13ffa3bf96f558755346d6e2e0dddf70`
- bin SHA-256：`ea442f2a09f1e961c87b313c4458527567cb540f3b29847d7d9041733d4a011b`
- hex SHA-256：`718748248321d881e9a7956664bec73ce4d1a8cb285a5e1ecf5b96b77a12120c`
- elf SHA-256：`b3bad6da1429f538b65660b225022f0b69b8607e4a1453adae20e6fbec278e35`
- ARM Release 构建、协议测试、yaw gate 测试和源码不变量检查均通过。
- **尚未烧录，尚未实车验证。**

D5 相对 D4 的核心修正：

- AUTO 恢复实车证明能收敛的 `yaw_speed_command = delta * 1.2f`。
- 输出上限恢复为 180，最小启动速度提高到 120。
- 增加启停滞回：误差大于 24 机械刻度（约 1.05°）启动，进入 10 刻度（约 0.44°）停止。
- 手动 yaw 改成 `-rc.ch[0]`，目标是右推右转、左推左转。
- XUC 协议、AUTO 安全门、串口超时保护和禁射逻辑没有改动。

详细依据见：

`firmware/stm32f405-xuc-v2/source/docs/XUC_PHASE2D5_YAW_CONVERGENCE_HANDOFF_20260903.md`

## 3. 协议与硬件事实

### XUC V2 数据包

小电脑 → 下位机，14 字节：

```cpp
#pragma pack(push, 1)
struct RxPacket_TJ {
  uint8_t head[2];       // 'S', 'P'
  uint8_t control_TJ;
  uint8_t shoot_TJ;
  float yaw_TJ;          // rad
  float pitch_TJ;        // rad
  uint16_t crc16_TJ;
};
#pragma pack(pop)
```

下位机 → 小电脑，20 字节：

```cpp
#pragma pack(push, 1)
struct TxPacket_TJ {
  uint8_t head[2];       // 'S', 'P'
  uint8_t mode_TJ;       // 0 idle, 1 auto aim, 2 small buff, 3 big buff
  uint8_t robot_id;      // 3 red, 103 blue
  float bullet_speed_TJ;
  uint16_t bullet_count_TJ;
  float imu_pitch_TJ;    // rad
  float imu_yaw_TJ;      // rad
  uint16_t crc16_TJ;
};
#pragma pack(pop)
```

两端包头均为 `SP`，CRC16 字节序为低字节在前。小电脑不再直接连接 IMU；IMU 接下位机，由 20 字节上行包提供姿态。

### 已确认硬件

- 下位机：STM32F405。
- 小电脑串口：CH340，Linux 稳定路径 `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`，解析到 `/dev/ttyUSB0`。
- 实际可用串口：下位机 USART6，460800 baud。此前 UART 接错/占用导致 `RX_BYTES=0`；换到 USART6 后约 100 个合法包/秒。
- 遥控器 DR16 接 USART3。模式拨杆正确时上报从 `mode=0` 变为 `mode=1`。
- 相机：Hikrobot MV-CS016-10UC。
- 调试器：正点原子 PTD02HW-Tx / CMSIS-DAP；Windows 曾枚举为 USB 复合设备和 COM7，VID/PID `04D8:00DF`。
- 旧 CH340 曾因误接冒烟，已经更换；重新接线前必须核对 TX/RX/GND 与电平，不能把 UART 模块接到 CANH/CANL。

## 4. 上位机状态

完整工程权威仓库：`kukuaxing/TJ-advanced`（私有）。

- 基线：`test/xuc-virtual-link`，`b8ef6d536ef0903a3b602d619bcc71db968b74fe`
- XUC V2 分支：`feat/xuc-board-communication-20260901`
- 已推送归档提交：`ed41007501b34ab2e13a125d7d2820a180829a33`
- 小电脑工作树：`~/sp_vision_25-xuc-board-20260901`

该分支已经实现：

- 14B 下行/20B 上行结构和编译期尺寸断言。
- CRC 拒绝、拆包、噪声重同步、反馈超时及恢复。
- IMU yaw/pitch 从 XUC 上行包进入上位机姿态链。
- 可禁用旧 CBoard 等待链路。
- `require_feedback_for_control`、`allow_control`、`allow_shoot` 三层安全门。
- `xuc_board_probe`、协议/安全 PTY 测试、接收诊断计数和接板手册。

归档仓库的 `upper-computer/TJ-advanced-xuc-v2/changed-files/` 是上述提交相对基线的 15 个文件快照，不是完整 TJ-advanced 工程。

重要：实车调试后，小电脑工作树曾出现未跟踪的 bench/Phase 2 配置，以及仅用于日志/录像的 `src/standard.cpp` 临时修改。新对话连接小电脑后，**第一件事必须运行 `git status --short --branch` 并备份这些文件；不要 reset、restore 或覆盖用户现场文件。** 已知配置名包括：

- `configs/standard_hikrobot_bench_xuc_v2.yaml`
- `configs/standard_hikrobot_bench_xuc_v2_exposure3.yaml`
- `configs/standard_hikrobot_phase2a_bounded_yaw.yaml`
- `configs/standard_hikrobot_phase2b_imu_yaw_sign_minus1.yaml`

最后用于实车 AUTO 测试的是 `standard_hikrobot_phase2b_imu_yaw_sign_minus1.yaml`。继续前仍要读取实际文件确认，不要只凭文件名推断参数。

## 5. 视觉侧已知结论

- 敌方颜色：red。
- `force_auto_aim: true`。
- `auto_fire: false`。
- 相机曝光从 6 ms 调到 3 ms 后稳定性明显改善；当前 bench 基准为 `exposure_ms: 3.0`、`gain: 10.0`。
- 一次固定画面统计：装甲板检测 612/750，81.6%。
- 一次移动统计：`ARMOR_RATE=80.7%`、`TARGET_RATE=96.7%`、`ARMOR_MAX_MISS=36`、`TARGET_MAX_MISS=21`，总计 1440 帧。
- 用户后续主观观察为“识别很稳定”。当前主要问题不再是识别，而是 yaw 控制方向、静摩擦和收敛。

不要为了控制问题随意放宽识别门限。先用日志证明目标角误差方向和电机响应，再做单因素控制调整。

## 6. 历次 yaw 实车证据

- Phase 2D.3（`4905e5965e35f84b5704d9f3acd44eaf5899aa04`）：AUTO 方向正确，目标 yaw 误差约从 `+16.82°` 降到 `+3.39°`；云台 yaw 约从 `-42.63°` 到 `-31.24°`。问题是约 3.4° 时静摩擦导致停止。
- Phase 2D.4（`331f080...`）：把电机输出改为 `-delta` 后发散，目标误差约从 `+11.63°` 增到 `+31.66°`，约 2 秒后丢目标。
- 手动左右两向均能运动，排除单向机械卡死、电机或 CAN 故障。
- 手动 DR16 曾表现为右推左转、左推右转，因此 D5 单独修正手动 `rc.ch[0]`；手动摇杆映射不能反推出 AUTO 坐标链符号。

## 7. 格式塔仿真可借鉴内容

最终仿真候选 v4q 位于 `simulation/gestalt-drive-aim-v4q/`：

- 反转：5/5，150 发，120 命中，80.0%。
- 正转：5/5，150 发，108 命中，72.0%。
- 合计：10/10，300 发，228 命中，76.0%。

可迁移的工程经验：

- 启动、接管、稳定保持分阶段处理。
- 用低带宽补偿消除执行器稳定偏置，不要让快速噪声直接驱动执行器。
- 使用启停滞回避免目标中心附近高频换向。
- 正反方向都必须做对称验证。
- 每次只改一个因素；无 RESULT、0 发和环境失败必须与算法命中率分开统计。

不可直接照搬：v4q 是格式塔/UE 执行器环境中的控制策略，并非 STM32 电机环参数。真实云台仍须以 IMU、目标误差和实车日志为依据。

## 8. 下一次继续工作的严格顺序

### A. 保护现场

1. 清空弹丸，禁用摩擦轮和拨弹机构。
2. 云台转动范围内无人、无线遥控可随时退出 AUTO。
3. 确认 `auto_fire: false`、`xuc_allow_shoot: false`。
4. 在小电脑检查 `standard` 没有运行、串口未被占用、ModemManager 已停用。
5. 保存小电脑 `git status`、当前配置和最新日志；不清理未跟踪文件。

### B. 烧录 D5

1. 使用本仓库 `firmware/stm32f405-xuc-v2/artifacts/pending-phase2d5/stm32f405_xuc_v2.bin`。
2. 烧录前核对 SHA-256 必须为 `ea442f2a09f1e961c87b313c4458527567cb540f3b29847d7d9041733d4a011b`。
3. OpenOCD 执行 program + verify + reset。
4. 回读 `0x08000000` 起同长度 41252 字节，再比对 SHA-256。
5. 保存命令、时间、固件提交、bin SHA、烧录日志和回读日志。

用户此前已经明确授权：后续烧录无需重复询问，只要满足安全准备、核对目标板与固件 SHA 并完整留档即可直接执行。若目标板身份、供电、接线或安全条件不确定，仍应停止而不是猜测。

### C. 通信与安全门复测

在小电脑运行 8 秒只接收探测：

```bash
cd ~/sp_vision_25-xuc-board-20260901
./build/xuc_board_probe \
  /dev/serial/by-id/usb-1a86_USB_Serial-if00-port0 \
  460800 \
  8
```

通过标准：`RX_BYTES>0`、`VALID_PACKETS>0`、`FIELD_ERRORS=0`，持续上报 IMU。随后运行 5 秒 `--safe-heartbeat`，`control=0`、`shoot=0` 时云台不得运动。

### D. D5 最小实车验证

1. 先退出 AUTO，用低幅摇杆确认：右推应右转、左推应左转。
2. 使用禁射配置启动上位机，装甲板先固定在画面左侧，再拨入 AUTO。
3. 核心判据不是“云台动了”，而是目标 yaw 误差绝对值连续减小并进入中心滞回区。
4. 目标误差连续增大、目标迅速离开画面、高频反复换向、撞限位或出现异响时立即退出 AUTO。
5. 左侧通过后，再依次测试右侧、中心保持、缓慢移动和正反方向重复。
6. 至少保存：上位机完整日志、模式切换时间、目标 yaw 误差、IMU yaw、控制命令和用户观察。

### E. 通过后的动作

只有 D5 左右两侧都能收敛并稳定保持，才能把它标记为“实车通过”。随后：

- 在固件仓库提交/打标签并记录实车结果。
- 把小电脑临时配置和必要诊断以独立提交整理到合适分支。
- 更新本归档仓库的 README、本文和 SHA256SUMS，再推送。
- pitch 解锁、快速移动目标、射击闭环分别作为后续独立阶段，不能与首次 yaw 收敛测试混做。

## 9. 仓库与路径索引

### 当前 Windows 电脑

- 汇总归档：`C:\Users\32751\Documents\GitHub\sp-vision-2026-integration-archive`
- 固件开发仓库：`C:\Users\32751\Documents\Codex\2026-09-01\files-mentioned-by-the-user-codex\RM_Infantry-Robot-xuc-v2-20260902`
- 仿真原始输出：`C:\Users\32751\Documents\Codex\2026-09-02\gestalt-simulation-handoff-2026-09-02\outputs\gestalt_drive_aim_2026-09-02`

### GitHub

- 汇总归档（私有）：`https://github.com/kukuaxing/sp-vision-2026-integration-archive`
- 上位机完整仓库（私有）：`https://github.com/kukuaxing/TJ-advanced`

### 小电脑

- 上位机工作树：`~/sp_vision_25-xuc-board-20260901`
- 接板日志：`~/sp_vision_25-xuc-board-20260901/logs/xuc-bringup/2026-09-02` 和 `2026-09-03`

## 10. 新对话的第一条执行准则

先确认“当前接的是哪台机器人、Flash 中是哪一个 SHA、工作树是否有未提交现场文件、发射机构是否安全”，再行动。当前最有价值的下一步不是继续改参数，而是安全烧录并验证 Phase 2D.5，用左右两侧的目标误差收敛曲线决定后续是否需要调最小启动速度、增益或滞回阈值。
