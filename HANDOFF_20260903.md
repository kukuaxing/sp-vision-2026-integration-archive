# 新对话交接：XUC V2 实车自瞄联调

更新时间：2026-09-05（Asia/Shanghai）

> 新对话请先完整阅读本文，再查看各子目录 README。不要根据阶段名称猜测实车状态；以本文“已烧录/未烧录”标记和 SHA-256 为准。

## 0AAA. 2026-09-05 夜间“小陀螺”移动靶归档（当前最高优先级）

> 本节是明日恢复实车工作的唯一当前入口，优先于 0AA 及以下全部历史章节。

- 四装甲“小陀螺”目标已完成旋转中心跟随和“一面一次”转火实弹初测。用户现场观察命中率约七成，但精确发数和逐发结果未记录，因此只标记为有前景、不得宣称正式验收通过。
- 上位机 `standard` 已停止。停止前下位机回报 `mode=0`、两侧摩擦轮 0 rpm、拨弹轮接近 0 rpm。剩余弹丸是否已全部取出仍须明日现场确认。
- 电控同学已把摩擦轮目标改为 6500 rpm；AUTO 稳态日志约 6440/6400 rpm。本轮没有对当前 Flash 做独立回读，精确固件文件和 SHA-256 未确认；下方旧的 3200 rpm 已烧录哈希不得冒充当前 Flash 身份。
- 实机代码已修正小陀螺字段错误（角速度应为 EKF `x[7]`，旧代码误用半径 `x[8]`），并使正反转都按角速度绝对值选择高速预测延迟。
- 小陀螺跟随改为 EKF 旋转中心；角速度门限 0.5 rad/s，旋转中心最多保持 3 s。保持期只能用于云台连续性，发射资格仍要求新鲜、收敛的 Tracker。
- 转火继承格式塔仿真的最小融合：0.20 s 高速预测、预测装甲面误差 ±12°、发射后锁止、34°或目标失效后重武装、一面一次。
- 低照度临时识别配置为曝光 6 ms、实际增益 16.01、门限 0.10。相机不接受增益 24；8 ms 曝光未改善检测率。
- 固定靶已验收的弹道参数保持不变：6500 rpm 配置弹速初值 19.36 m/s、参考总落差 0.08639 m、Pitch trim -0.96°。
- 完整源码、二进制、配置、八份日志、诊断图与明日测试矩阵见 `upper-computer/TJ-advanced-xuc-v2/field-results/2026-09-05-gyro-moving-target/README.md`。
- 明日必须先核对安全档、清空弹丸、当前 Flash 哈希，再依次运行禁控诊断、禁射跟随。实弹固定为反转 10 发和正转 10 发，逐发记录；先保持参数不变，再依据方向分组结果决定是否单因素调整 0.20 s 预测延迟。

## 0AA. 2026-09-05 视觉调试暂停状态（历史记录，次于 0AAA）

> 本节曾是有效恢复入口，现已由上方 0AAA 取代；其余过程记录仍保留。0A 中“600 ms/供弹锁存固件尚未烧录”的描述已经过时。

### 当前停机与接管状态

- 现场人员已确认机器人回到真正安全档；上位机 `standard` 及相关监控进程已停止。
- 视觉调试暂停，电控同学接管机器人调试。不得因阅读到下方旧的试射步骤而启动 AUTO、摩擦轮、拨弹轮或上位机视觉程序。
- 停止前相机与 CH340 均能枚举；这不代替下一次恢复时的重新检查。
- 小电脑 `/home/rm/sp_vision_25-xuc-board-20260901` 的启用配置已恢复到 12.8 m/s 安全基线。实验性的 2.7 m 修正配置另存为 `*_UNVALIDATED.yaml`，不得直接用于实弹。
- 下一次视觉接管必须先由现场确认：电控调试结束、机器人回真正安全档、弹仓和拨弹轮内弹丸状态明确、人员离开发射方向。

### 当前 Flash 中的固件

- 已烧录：3200 rpm + 600 ms 节拍 + 完整 45° 供弹锁存 + 摩擦轮就绪门控版。
- 行为：摩擦轮达到目标转速 90% 并持续 200 ms 才允许供弹；低于 70% 暂停，恢复到 90% 后继续；AUTO 退出、遥控失效或串口过期时复位发射状态。
- 文件：`firmware/stm32f405-xuc-v2/artifacts/flashed-phase2d5-rpm3200-cadence600-feed-latch-20260905/stm32f405_xuc_v2_rpm3200_cadence600_feed_latch.bin`。
- 长度：42,692 字节。
- SHA-256：`AAAF4C33531B37DD720B811DB5C4E571B807B429BAC18E6F51DC37D627862571`。
- 同目录 `readback_42692.bin` 长度和 SHA-256 完全相同；主机测试、ARM Release 构建、烧录、校验、独立回读与复位运行均成功。

### 实车验收与故障结论

- 空仓验收通过。
- 首轮实际发射 5 发，落点：下、下、右下、右下、右下。
- 后续一次首发时相机 USB 掉线；内核记录 `usbfs -19`、USB 2-2 disconnect、端口描述符 `-110`。机械同学固定相机线后，12 秒禁射隔离、单次空拨弹、单发实弹及后续两轮 5 发均未复现掉线。
- 固定线缆后的单发落点为下；最后一轮 5 发落点为左下、左下、下、左、左下。
- 最后一轮靶距 2.7 m，弹着群中心约向左 10 cm、向下 20 cm。
- 按该偏差计算，几何修正约向右 2.121°、向上 4.236°；由弹道反算的等效弹速约 9.53 m/s。
- 9.5 m/s 空弹修正试验使 Pitch 到约 -14.16°，目标从画面下方离开，未取得有效计数。该方案未通过验证，不能实弹；不得绕过失去目标后的禁射保护。
- 若要完整补偿，应先评估将相机光轴相对炮管向下调整约 4°，之后重做相关外参；否则只能采用部分补偿。

### 上位机最终快照

- `src/standard.cpp` 已加入可选的 `xuc_direct_yaw_impact_correction_deg`，默认值为 0，并已构建成功；SHA-256：`2C96BAAEA61C4233FAAF1EB3884E83F06BB6060649C40CF0CFB84DC9575BE094`。
- 当前启用实弹基线配置 SHA-256：`A184BD38364E848F045C0E92F45E0D9DDBA252D388A5D7C3ECE57EDA23BE2EDD`。
- 当前启用空弹基线配置 SHA-256：`A07C35E9F55157667532137353D2F35C211B6DCE37D100A98D3952099D10C211`。
- 详细结果、8 组日志及监控文件见 `upper-computer/TJ-advanced-xuc-v2/field-results/2026-09-05/README.md`。

## 0A. 2026-09-05 最新现场状态（最高优先级）

> 本节优先于本文其余全部历史内容。9 月 4 日“下一步为独立开火验收”已经完成并继续推进到固定靶弹道与射击散布诊断。不要按旧章节重新做相机标定、双轴初调或 2500 rpm 开火。

### 一句话状态

- 双轴自动跟踪、蓝色装甲板识别、基本开火链和 3200 rpm 固定靶弹道已经打通。
- 2.05 m 固定靶中，正常弹可命中装甲板中部，但有较大概率散到右下角。
- 日志显示开火瞬间视觉残差很小，因此当前首要假设是连续供弹太快导致摩擦轮来不及恢复或两轮恢复不一致，而不是继续修改瞄准偏置。
- 已按单因素原则完成“3200 rpm 不变、每发至少间隔 600 ms、记录两侧摩擦轮转速”的代码和构建；**该下位机固件尚未烧录、尚未实车测试**。
- 小电脑上的对应转速日志代码已更新并构建；`standard` 当前已停止。
- 用户最后确认遥控器已回真正安全档；最后日志也是 `mode=0`。但弹丸是否已清空没有最新确认，下一次连接后必须现场确认，不能猜。

### 必须纠正的烧录设备事实

- **烧录使用的是 Windows 设备管理器中枚举为 COM7 的无线调试器。不要再要求 ST-Link。**
- 该设备同时提供 CMSIS-DAP/SWD 能力；此前成功烧录与回读使用的是同一个 COM7 无线调试器对应的 CMSIS-DAP 探针以及 pyOCD/OpenOCD，而不是 ST-Link。
- COM7 是现场人员确认、插拔后可复现的设备身份。下一次应先检查 COM7/该无线调试器是否出现，再让 pyOCD 枚举同一个 CMSIS-DAP 探针；不要因 pyOCD 走 SWD 就把设备误称为 ST-Link。
- 当前可能未插着无线调试器；本次交接整理期间 Windows 串口枚举为空，没有执行任何烧录。

### 真正安全档与 AUTO 档

- 真正安全档：DR16 左拨杆 `MID`、右拨杆 `MID`，下位机 `RESET`，上报 `mode=0`。
- AUTO：DR16 左拨杆 `MID`、右拨杆 `DOWN`，`XucArmSwitchSelected()` 为真，上报 `mode=1`。
- 用户已经多次纠正：这里说的是遥控器两个拨杆，不是鼠标，也不是随意称呼的“安全地带”。
- 实弹前完整条件仍是：真正安全档、挡弹设施就绪、射界无人、底盘架空、弹丸按本轮数量装填、装甲板稳定；异常立即回真正安全档。

### 当前机器人里已烧录的固件（仍在 Flash 中）

- 功能：既有双轴跟踪/连续开火逻辑，摩擦轮目标从 2500 提高到 `3200 rpm`；尚无 600 ms 发射间隔和双轮独立遥测。
- 文件：`C:\Users\32751\Documents\Codex\2026-09-03\https-github-com-kukuaxing-sp-vision-2\outputs\phase2d5_rpm3200_fixed_target_20260905\stm32f405_xuc_v2_rpm3200.bin`
- 长度：`42340` 字节。
- bin SHA-256：`B0A21B9809BA8FF4A1130255FAFE5BFA3EEB43A0F1B38D072D86D41FC06EB36A`。
- 同目录 `readback.bin` 长度和 SHA-256 完全相同，已经独立回读验证。
- `flash.log`/`readback.log` 记录了本次成功 pyOCD 烧录、回读和复位。

### 下一待烧录固件：3200 rpm + cadence600（严禁混淆）

- 文件：`C:\Users\32751\Documents\Codex\2026-09-03\https-github-com-kukuaxing-sp-vision-2\outputs\phase2d5_rpm3200_cadence600_20260905\stm32f405_xuc_v2_rpm3200_cadence600.bin`
- 长度：`42468` 字节。
- SHA-256：`BEC6DE2C6F301BDC51DAC95883C120DC32E9B149D143782396D42E5B112D51F9`。
- 状态：ARM Release 构建成功；协议、CRC、Yaw gate、Pitch gate、AUTO fire gate 等 5 项既有主机回归测试通过；**未烧录、未回读、未上电测试**。
- 下位机源码工作树：`C:\Users\32751\Documents\Codex\2026-09-01\files-mentioned-by-the-user-codex\RM_Infantry-Robot-xuc-v2-20260902`。
- 分支仍是 `fix/xuc-v2-yaw-convergence-phase2d5-20260903`，存在大量实车累计修改和未跟踪构建产物。不要 reset/restore/clean，不要假设 HEAD 就是现场代码。

该待测固件只针对本轮散布诊断新增以下内容：

1. `shoot_speed` 继续保持 `3200 rpm`，上位机假定弹速继续保持 `12.8 m/s`；双轴跟踪和弹道补偿不改。
2. `AUTO_SHOT_INTERVAL_MS = 600`：第一发可以立即推进；每推进一个 45° 弹位后停止拨弹轮至少 600 ms，再允许下一发。
3. 摩擦轮在 AUTO 中仍持续转动；600 ms 只暂停拨弹轮，不会把摩擦轮停掉。
4. 间隔状态跨越上位机 `shoot` 的短暂撤销/重新满足，避免靠门限抖动绕过恢复时间。
5. `ctrl.shooter.wheel0_rpm`、`wheel1_rpm`、`wheel_rpm_delta` 保存两侧 M3508 实际转速绝对值，可供 Live Watch。
6. 20 字节 XUC 包尺寸与 CRC 不变：`bullet_speed_TJ` 继续携带拨弹 M2006 实际 RPM；`bullet_count_TJ` 临时改为双轮转速打包，低 8 位是 wheel0、高 8 位是 wheel1，量化为 `20 rpm/LSB`。

### 小电脑的精确状态

- SSH：`rm@192.168.50.2`；密码属于凭据，只在当前聊天中由用户提供，不写入仓库，新对话若不能复用连接应向用户索取。
- 工程：`/home/rm/sp_vision_25-xuc-board-20260901`。
- 分支：`feat/xuc-board-communication-20260901`，相对远端存在大量已修改和未跟踪的标定、配置与现场日志。不得 reset、restore、clean 或覆盖现场文件。
- `standard` 当前没有运行；本次交接前已在 `mode=0` 下向 PID 79779 发送 SIGINT 并确认退出。
- 当前 `src/standard.cpp` 已加入 cadence600 配套双轮日志并完成 `cmake --build build -j2`：mode=1 时约每 5 帧打印一次 `feeder_rpm/wheel0_rpm/wheel1_rpm/wheel_delta`，mode=0 时每 50 帧打印一次。
- 当前 `src/standard.cpp` SHA-256：`F89AC87471F561B6C6BEB5488D8F58080DC14F1177B79D75809C95DF8D671B59`。
- 备份：`src/standard.cpp.pre-cadence600-20260905`。
- 实弹配置：`configs/standard_hikrobot_phase2d5_fire_single.yaml`，SHA-256 `A184BD38364E848F045C0E92F45E0D9DDBA252D388A5D7C3ECE57EDA23BE2EDD`。
- 空弹配置：`configs/standard_hikrobot_phase2d5_fire_dryrun.yaml`，SHA-256 `A07C35E9F55157667532137353D2F35C211B6DCE37D100A98D3952099D10C211`。
- 最新完整日志：`logs/xuc-bringup/2026-09-05/blue_fixed_rpm3200_round1.log`。结束前连续上报 `mode=0`、拨弹轮约 0 rpm。

重新启动上位机需要桌面相机画面时使用现有图形会话环境：

```bash
cd /home/rm/sp_vision_25-xuc-board-20260901
DISPLAY=:0 \
XAUTHORITY=/run/user/1000/.mutter-Xwaylandauth.C9PBV3 \
XDG_RUNTIME_DIR=/run/user/1000 \
DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus \
./build/standard configs/standard_hikrobot_phase2d5_fire_single.yaml
```

应重定向到新的、带 cadence600 名称的日志文件，不要覆盖 `blue_fixed_rpm3200_round1.log`。

### 当前视觉、标定和双轴控制基线

- 当前敌方颜色/移动靶灯条颜色：`blue`。此前确实发生过 BGR/RGB 通道反转，已经修复；不要再切回 red。
- 相机：Hikrobot `MV-CS016-10UC`，`1440x1080` 全画幅，曝光 `2.0 ms`，gain `10`，`BayerRG8`，软件触发关闭；实测画面可实时。
- 曝光 1--10 ms 扫描和人工调光圈后，2 ms 识别最好。蓝色检测框稳定，绿色预测框曾有轻微跳动。
- 相机曾出现非常严重的旧帧/延迟现象；重新插拔相机后恢复实时，随后双轴跟踪再次顺滑。若再次出现“云台已经动了但画面过很久才变化”，先处理相机/USB/缓冲问题，不要误调 PID。
- 图像处理是实时内存流水线，不是“先把每帧存盘再处理”。磁盘不足来自累计的标定图、诊断输出和日志；可定期清理明确可再生的运行日志/诊断图，但标定原图、选定内外参和最新实车日志必须先备份，禁止无差别删除。
- 当前内参重投影误差 `0.2769 px`；配置中的 `camera_matrix`、`distort_coeffs`、`R_camera2gimbal`、`t_camera2gimbal` 已用于通过的双轴测试。
- 相机镜头是固定 `8 mm`、`2/3"`、手动光圈/手动对焦镜头，无变焦环；当前已使用传感器全分辨率，软件无法扩大物理视场。换更短焦镜头后必须重做内参与手眼/外参。
- Yaw：30 ms 测量滤波、120 deg/s 上限、300 ms 短丢帧保持；下位机目标相对解锁原点 ±25°、实际 ±28°硬保护。
- Pitch：像素误差增益 `1.65`、30 ms 滤波、80 deg/s 上限、180 ms 短丢帧保持；代码当前目标相对原点 ±25°、实际 ±28°硬保护，电机软限位约 `[-0.45, +0.4] rad`。配置里仍写“±15°”的注释已过时，以代码为准。
- 双轴跟踪的最终实车结论：Yaw/Pitch 运动顺滑，无持续振荡；完整往返与双轴轨迹已通过。准备开火期间曾因相机旧帧表现出振荡，重插相机并恢复已通过的跟踪逻辑后恢复正常。因此不要再改已通过的双轴控制逻辑。

### 当前开火与弹道基线

- 下位机摩擦轮：M3508，CAN1 ID1/ID4，目标 `±3200 rpm`，固件最大允许 3800 rpm；速度 PID 为 `(Kp=4, Ki=0, Kd=1.5)`，没有积分项，受负载后可能存在稳态掉速。
- 拨弹轮：M2006，CAN1 ID5，目标 `2160 rpm`；8 槽弹盘，每发推进 45°，即电机累计编码器 `36864 ticks`。
- AUTO 基本策略按用户要求简化：AUTO 有效时摩擦轮不允许停转；视觉满足基本开火条件时拨弹。底盘在 AUTO 始终命令为 0。
- 上位机开火基本门限：新鲜装甲板测量、Yaw 误差 ≤3.0°、Pitch 误差 ≤1.5°、连续稳定 200 ms。不要重新叠加复杂门控。
- 实测几何：相机光心相对炮口向前 6 cm、向上 4 cm。
- 0.91 m/2500 rpm 的实弹参考总抬高量为 17 cm；该参考按 `10.0 m/s` 固定保存。
- 当前 3200 rpm 对应的 `xuc_projectile_speed_mps=12.8` 是按 2500 rpm≈10 m/s 线性换算的初值，**没有测速仪实测**。
- 弹道补偿上限为 20°。提高到 3200 rpm 后，约 2.05 m 目标所需抬高减少，装甲板重新留在相机画面内；不要用 Pitch 机械限位去解决镜头视场问题。
- 2.05 m 固定靶最新结果：正常弹刚好命中中部，但较大概率偏右下。
- 同一轮日志中，开火资格出现时视觉残差通常为 Yaw `<0.1°`、Pitch `<约0.14°`；这强烈说明随机右下散布不是静态视觉瞄偏。向下符合某发出口速度下降，向右符合两轮转速/压紧/摩擦不一致或供弹姿态差异。
- 3200 rpm 低于 3800 rpm 固件上限，当前不应先降转速或改弹道平均偏置。先用 cadence600 和双轮转速日志判断恢复与一致性。

### 本轮重要实车结论回顾

1. 相机标定、手眼/外参采集与验证已经完成；除非镜头、相机固定方式或焦距改变，不要重做。
2. Yaw 最初存在方向错误、过冲、卡顿与振荡；最终改为复用电控原有位置-速度串级控制并调好上位机连续目标，v5 完整往返无振荡、卡顿不严重。
3. Pitch 符号曾反，修正后向目标中心运动；速度从 50、60--70 提到 80 deg/s 后仍顺滑并基本居中。
4. 双轴联调通过后加入开火曾出现 Pitch 振荡；最终发现主要诱因是相机严重延迟，不是应继续改已保存的双轴 PID。相机重插恢复实时后双轴再次顺滑。
5. 摩擦轮/拨弹早期不工作同时包含电控固件被替换、CH340/4-pin 接线、主控未上电和机械卡滞等因素；电控代码曾先连发不动、排除机械问题后连发成功。之后重新烧录本项目固件，拨弹已能正常转动。
6. 固定靶和移动靶开火已经打通；移动靶存在跟随/转火不足，当前先回到固定靶提高散布与一致性，再继续移动靶命中率。
7. 用户明确要求按格式塔仿真的工程经验推进：每次只改一个因素、区分环境/机械失败和算法命中率、保留已通过基线；不要再用大量没有信息增益的“蠕动式小测试”拖慢进度。

### 新对话必须按此顺序继续

1. 先完整阅读本节；不得根据旧章节把当前固件误认成 9 月 4 日双轴禁射版。
2. 只读核对 Windows 下待测 bin 的长度和 SHA-256，以及下位机工作树的上述四个 cadence/telemetry 修改；不要清理工作树。
3. 现场确认：真正安全档、弹丸已清空、底盘架空、COM7 无线调试器已连接。**不要要求 ST-Link。**
4. 停止状态下使用 COM7 无线调试器对应的 CMSIS-DAP/SWD 探针烧录 cadence600 bin；先 program+verify，再从 `0x08000000` 回读精确 `42468` 字节，SHA-256 必须等于 `BEC6...D51F9`，最后 reset/run并保存日志。
5. 先做必要且一次性的空仓安全验收：真正安全档必须底盘、云台、摩擦轮、拨弹全无动作；AUTO 下摩擦轮应持续转，只有装甲板满足门限时拨弹轮才每发推进一次且相邻完成时刻 ≥600 ms。该步骤用于验证新改动，不属于重复蠕动调参。
6. 从新日志读取 `wheel0_rpm/wheel1_rpm/wheel_delta/feeder_rpm`。先确认两轮空载能稳定接近 3200，再看每次拨弹后的最低转速、两轮差和恢复时间。
7. 空仓通过后再按完整实弹安全条件装 5 发，在同一约 2.05 m 固定靶重复一轮；记录每发落点顺序和是否仍偏右下，并保存完整日志。
8. 若 cadence600 明显缩小散布，保持 3200 和现有弹道；再依据日志把间隔缩短到仍能完全恢复的最小值。若两轮明显不一致，再做每侧目标/PID或机械压紧修正。若两轮稳定一致而仍右下散布，优先检查胶轮压紧量、磨损/同心度、供弹姿态、枪管和弹丸一致性。
9. 在固定靶散布问题收敛前，不要同时改预测、跟踪增益、弹速、弹道偏置和射击门限；之后再回到蓝色移动靶转火/提前量调试。

## 0. 2026-09-04 最新状态（优先于下文历史交接）

下文第 1--8 节保留 2026-09-03 的历史语境；当其“当前/下一步”与本节冲突时，以本节为准。

### 已完成

- Phase 2D.5 已烧录、独立回读一致，并完成 Yaw、Pitch 和双轴实车联调。
- 当前机器人固件 bin SHA-256：`7708D30EE510AB4807379175C4314B1D413BD3F96CB01A3F93A92B07937A1F21`，长度 41988 字节。
- 固件产物与回读：`firmware/stm32f405-xuc-v2/artifacts/flashed-phase2d5-joint-20260904/`。
- 最终双轴配置：`upper-computer/TJ-advanced-xuc-v2/changed-files/configs/standard_hikrobot_phase2d5_joint_acceptance.yaml`。
- 双轴完整日志与结论：`upper-computer/TJ-advanced-xuc-v2/field-results/2026-09-04/`。
- Yaw 左右/中心/往返/快移跟随通过；Pitch 上下往返以 1.65 像素误差增益、80 deg/s 上限通过；双轴“∞”轨迹联调通过。
- 双轴实车观感：跟随顺利，无持续振荡，稳定后基本居中。

### 当前参数和保护

- 相机：Hikrobot 1440x1080，曝光 2 ms，gain 10，约 65--68 FPS。
- Yaw 上位机测量滤波 30 ms，连续变化上限 120 deg/s，丢帧保持 180 ms，预测超前为 0。
- Pitch 使用装甲板像素纵向误差闭环，增益 1.65，滤波 30 ms，连续变化上限 80 deg/s，丢帧保持 180 ms。
- 下位机 Yaw 目标 ±25 deg、实际 ±28 deg 硬锁止；Pitch 目标 ±15 deg、实际 ±18 deg 硬锁止、斜坡 80 deg/s。
- 已烧录后复测：803 个有效包/8 s，字段错误 0；500 个安全心跳/5 s，字段错误 0。
- 截至双轴验收，`auto_fire: false`、`xuc_allow_shoot: false`，底盘、摩擦轮和拨弹未由视觉链路启用。

### 下一步：独立开火验收

1. 真正安全档，上位机 `standard` 停止。
2. 确认枪口指向合格靶区，人员离开射线和云台活动区，底盘架空，遥控可随时退出 AUTO。
3. 先不装弹验证摩擦轮和拨弹的遥控/下位机逻辑，确认安全档无动作。
4. 建立独立开火配置；不要直接覆盖已通过的双轴禁射配置。
5. 先只允许摩擦轮预热，再单独验证一次单发拨弹，最后才允许视觉触发；每步完成后回真正安全档。
6. 开火任何异常（连发、拨弹卡滞、摩擦轮异响、枪口移出靶区、丢目标）立即退出 AUTO 并断能。

## 1. 最终目标与当前结论

目标是让小电脑识别红色装甲板后，控制 yaw 云台跟随，使装甲板稳定收敛到画面中心；射击暂不在本轮范围内。

当前已经打通：

- 海康相机取流和红色装甲板识别。
- 小电脑经 CH340 与下位机 USART6 的 460800 波特率双向 XUC V2 通信。
- 下位机 IMU 的 yaw/pitch 上报、CRC、丢包/超时保护和上位机诊断。
- 下位机接收 control/yaw/pitch 指令并能实际驱动 yaw 云台。
- DR16 模式切换能够让下位机上报 `mode=1`，上位机进入 `auto_aim`。

尚未完成：

- yaw 自动跟随的最终收敛验证。Phase 2D.3 曾证明正确 AUTO 符号能够收敛，但存在约 3.4° 静摩擦残差；Phase 2D.4 改错符号后会发散。
- Phase 2D.5 已针对上述问题完成代码和构建，尚未烧录、尚未实车验证。
- pitch 云台当时机械锁死，本轮没有验证 pitch 跟随。
- 摩擦轮、拨弹和实弹闭环全部未启用。

## 2. 绝不能混淆的版本状态

### 当前机器人内的固件：Phase 2D.4

- 提交：`331f080c9dd0d213e513a5c0d412421aca2ebae8`
- bin SHA-256：`cf13b6e7ea65106bd1f43ba8be39daf64236a4e5e9e90e3df88454fd5151173d`
- 已执行 OpenOCD program/verify，并做过同长度 Flash 回读比对。
- 实车结论：通信正常、云台能动，但 AUTO 符号错误，会把装甲板推向画面边缘。**不要继续用 D4 做跟随调参。**

### 下一待测固件：Phase 2D.5

- 固件代码提交：`4fb76dbba388e9fb09e2dd8b8b8ed1d253d92105`
- 含交接文档的源码快照 HEAD：`5ab5e2af13ffa3bf96f558755346d6e2e0dddf70`
- bin SHA-256：`ea442f2a09f1e961c87b313c4458527567cb540f3b29847d7d9041733d4a011b`
- hex SHA-256：`718748248321d881e9a7956664bec73ce4d1a8cb285a5e1ecf5b96b77a12120c`
- elf SHA-256：`b3bad6da1429f538b65660b225022f0b69b8607e4a1453adae20e6fbec278e35`
- ARM Release 构建、协议测试、yaw gate 测试和源码不变量检查均通过。
- **尚未烧录，尚未实车验证。**

D5 相对 D4 的核心修正：

- AUTO 恢复实车证明能收敛的 `yaw_speed_command = delta * 1.2f`。
- 输出上限恢复为 180，最小启动速度提高到 120。
- 增加启停滞回：误差大于 24 机械刻度（约 1.05°）启动，进入 10 刻度（约 0.44°）停止。
- 手动 yaw 改成 `-rc.ch[0]`，目标是右推右转、左推左转。
- XUC 协议、AUTO 安全门、串口超时保护和禁射逻辑没有改动。

详细依据见：

`firmware/stm32f405-xuc-v2/source/docs/XUC_PHASE2D5_YAW_CONVERGENCE_HANDOFF_20260903.md`

## 3. 协议与硬件事实

### XUC V2 数据包

小电脑 → 下位机，14 字节：

```cpp
#pragma pack(push, 1)
struct RxPacket_TJ {
  uint8_t head[2];       // 'S', 'P'
  uint8_t control_TJ;
  uint8_t shoot_TJ;
  float yaw_TJ;          // rad
  float pitch_TJ;        // rad
  uint16_t crc16_TJ;
};
#pragma pack(pop)
```

下位机 → 小电脑，20 字节：

```cpp
#pragma pack(push, 1)
struct TxPacket_TJ {
  uint8_t head[2];       // 'S', 'P'
  uint8_t mode_TJ;       // 0 idle, 1 auto aim, 2 small buff, 3 big buff
  uint8_t robot_id;      // 3 red, 103 blue
  float bullet_speed_TJ;
  uint16_t bullet_count_TJ;
  float imu_pitch_TJ;    // rad
  float imu_yaw_TJ;      // rad
  uint16_t crc16_TJ;
};
#pragma pack(pop)
```

两端包头均为 `SP`，CRC16 字节序为低字节在前。小电脑不再直接连接 IMU；IMU 接下位机，由 20 字节上行包提供姿态。

### 已确认硬件

- 下位机：STM32F405。
- 小电脑串口：CH340，Linux 稳定路径 `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`，解析到 `/dev/ttyUSB0`。
- 实际可用串口：下位机 USART6，460800 baud。此前 UART 接错/占用导致 `RX_BYTES=0`；换到 USART6 后约 100 个合法包/秒。
- 遥控器 DR16 接 USART3。模式拨杆正确时上报从 `mode=0` 变为 `mode=1`。
- 相机：Hikrobot MV-CS016-10UC。
- 调试器：正点原子 PTD02HW-Tx / CMSIS-DAP；Windows 曾枚举为 USB 复合设备和 COM7，VID/PID `04D8:00DF`。
- 旧 CH340 曾因误接冒烟，已经更换；重新接线前必须核对 TX/RX/GND 与电平，不能把 UART 模块接到 CANH/CANL。

## 4. 上位机状态

完整工程权威仓库：`kukuaxing/TJ-advanced`（私有）。

- 基线：`test/xuc-virtual-link`，`b8ef6d536ef0903a3b602d619bcc71db968b74fe`
- XUC V2 分支：`feat/xuc-board-communication-20260901`
- 已推送归档提交：`ed41007501b34ab2e13a125d7d2820a180829a33`
- 小电脑工作树：`~/sp_vision_25-xuc-board-20260901`

该分支已经实现：

- 14B 下行/20B 上行结构和编译期尺寸断言。
- CRC 拒绝、拆包、噪声重同步、反馈超时及恢复。
- IMU yaw/pitch 从 XUC 上行包进入上位机姿态链。
- 可禁用旧 CBoard 等待链路。
- `require_feedback_for_control`、`allow_control`、`allow_shoot` 三层安全门。
- `xuc_board_probe`、协议/安全 PTY 测试、接收诊断计数和接板手册。

归档仓库的 `upper-computer/TJ-advanced-xuc-v2/changed-files/` 是上述提交相对基线的 15 个文件快照，不是完整 TJ-advanced 工程。

重要：实车调试后，小电脑工作树曾出现未跟踪的 bench/Phase 2 配置，以及仅用于日志/录像的 `src/standard.cpp` 临时修改。新对话连接小电脑后，**第一件事必须运行 `git status --short --branch` 并备份这些文件；不要 reset、restore 或覆盖用户现场文件。** 已知配置名包括：

- `configs/standard_hikrobot_bench_xuc_v2.yaml`
- `configs/standard_hikrobot_bench_xuc_v2_exposure3.yaml`
- `configs/standard_hikrobot_phase2a_bounded_yaw.yaml`
- `configs/standard_hikrobot_phase2b_imu_yaw_sign_minus1.yaml`

最后用于实车 AUTO 测试的是 `standard_hikrobot_phase2b_imu_yaw_sign_minus1.yaml`。继续前仍要读取实际文件确认，不要只凭文件名推断参数。

## 5. 视觉侧已知结论

- 敌方颜色：red。
- `force_auto_aim: true`。
- `auto_fire: false`。
- 相机曝光从 6 ms 调到 3 ms 后稳定性明显改善；当前 bench 基准为 `exposure_ms: 3.0`、`gain: 10.0`。
- 一次固定画面统计：装甲板检测 612/750，81.6%。
- 一次移动统计：`ARMOR_RATE=80.7%`、`TARGET_RATE=96.7%`、`ARMOR_MAX_MISS=36`、`TARGET_MAX_MISS=21`，总计 1440 帧。
- 用户后续主观观察为“识别很稳定”。当前主要问题不再是识别，而是 yaw 控制方向、静摩擦和收敛。

不要为了控制问题随意放宽识别门限。先用日志证明目标角误差方向和电机响应，再做单因素控制调整。

## 6. 历次 yaw 实车证据

- Phase 2D.3（`4905e5965e35f84b5704d9f3acd44eaf5899aa04`）：AUTO 方向正确，目标 yaw 误差约从 `+16.82°` 降到 `+3.39°`；云台 yaw 约从 `-42.63°` 到 `-31.24°`。问题是约 3.4° 时静摩擦导致停止。
- Phase 2D.4（`331f080...`）：把电机输出改为 `-delta` 后发散，目标误差约从 `+11.63°` 增到 `+31.66°`，约 2 秒后丢目标。
- 手动左右两向均能运动，排除单向机械卡死、电机或 CAN 故障。
- 手动 DR16 曾表现为右推左转、左推右转，因此 D5 单独修正手动 `rc.ch[0]`；手动摇杆映射不能反推出 AUTO 坐标链符号。

## 7. 格式塔仿真可借鉴内容

最终仿真候选 v4q 位于 `simulation/gestalt-drive-aim-v4q/`：

- 反转：5/5，150 发，120 命中，80.0%。
- 正转：5/5，150 发，108 命中，72.0%。
- 合计：10/10，300 发，228 命中，76.0%。

可迁移的工程经验：

- 启动、接管、稳定保持分阶段处理。
- 用低带宽补偿消除执行器稳定偏置，不要让快速噪声直接驱动执行器。
- 使用启停滞回避免目标中心附近高频换向。
- 正反方向都必须做对称验证。
- 每次只改一个因素；无 RESULT、0 发和环境失败必须与算法命中率分开统计。

不可直接照搬：v4q 是格式塔/UE 执行器环境中的控制策略，并非 STM32 电机环参数。真实云台仍须以 IMU、目标误差和实车日志为依据。

## 8. 下一次继续工作的严格顺序

### A. 保护现场

1. 清空弹丸，禁用摩擦轮和拨弹机构。
2. 云台转动范围内无人、无线遥控可随时退出 AUTO。
3. 确认 `auto_fire: false`、`xuc_allow_shoot: false`。
4. 在小电脑检查 `standard` 没有运行、串口未被占用、ModemManager 已停用。
5. 保存小电脑 `git status`、当前配置和最新日志；不清理未跟踪文件。

### B. 烧录 D5

1. 使用本仓库 `firmware/stm32f405-xuc-v2/artifacts/pending-phase2d5/stm32f405_xuc_v2.bin`。
2. 烧录前核对 SHA-256 必须为 `ea442f2a09f1e961c87b313c4458527567cb540f3b29847d7d9041733d4a011b`。
3. OpenOCD 执行 program + verify + reset。
4. 回读 `0x08000000` 起同长度 41252 字节，再比对 SHA-256。
5. 保存命令、时间、固件提交、bin SHA、烧录日志和回读日志。

用户此前已经明确授权：后续烧录无需重复询问，只要满足安全准备、核对目标板与固件 SHA 并完整留档即可直接执行。若目标板身份、供电、接线或安全条件不确定，仍应停止而不是猜测。

### C. 通信与安全门复测

在小电脑运行 8 秒只接收探测：

```bash
cd ~/sp_vision_25-xuc-board-20260901
./build/xuc_board_probe \
  /dev/serial/by-id/usb-1a86_USB_Serial-if00-port0 \
  460800 \
  8
```

通过标准：`RX_BYTES>0`、`VALID_PACKETS>0`、`FIELD_ERRORS=0`，持续上报 IMU。随后运行 5 秒 `--safe-heartbeat`，`control=0`、`shoot=0` 时云台不得运动。

### D. D5 最小实车验证

1. 先退出 AUTO，用低幅摇杆确认：右推应右转、左推应左转。
2. 使用禁射配置启动上位机，装甲板先固定在画面左侧，再拨入 AUTO。
3. 核心判据不是“云台动了”，而是目标 yaw 误差绝对值连续减小并进入中心滞回区。
4. 目标误差连续增大、目标迅速离开画面、高频反复换向、撞限位或出现异响时立即退出 AUTO。
5. 左侧通过后，再依次测试右侧、中心保持、缓慢移动和正反方向重复。
6. 至少保存：上位机完整日志、模式切换时间、目标 yaw 误差、IMU yaw、控制命令和用户观察。

### E. 通过后的动作

只有 D5 左右两侧都能收敛并稳定保持，才能把它标记为“实车通过”。随后：

- 在固件仓库提交/打标签并记录实车结果。
- 把小电脑临时配置和必要诊断以独立提交整理到合适分支。
- 更新本归档仓库的 README、本文和 SHA256SUMS，再推送。
- pitch 解锁、快速移动目标、射击闭环分别作为后续独立阶段，不能与首次 yaw 收敛测试混做。

## 9. 仓库与路径索引

### 当前 Windows 电脑

- 汇总归档：`C:\Users\32751\Documents\GitHub\sp-vision-2026-integration-archive`
- 固件开发仓库：`C:\Users\32751\Documents\Codex\2026-09-01\files-mentioned-by-the-user-codex\RM_Infantry-Robot-xuc-v2-20260902`
- 仿真原始输出：`C:\Users\32751\Documents\Codex\2026-09-02\gestalt-simulation-handoff-2026-09-02\outputs\gestalt_drive_aim_2026-09-02`

### GitHub

- 汇总归档（私有）：`https://github.com/kukuaxing/sp-vision-2026-integration-archive`
- 上位机完整仓库（私有）：`https://github.com/kukuaxing/TJ-advanced`

### 小电脑

- 上位机工作树：`~/sp_vision_25-xuc-board-20260901`
- 接板日志：`~/sp_vision_25-xuc-board-20260901/logs/xuc-bringup/2026-09-02` 和 `2026-09-03`

## 10. 新对话的第一条执行准则

先确认“当前接的是哪台机器人、Flash 中是哪一个 SHA、工作树是否有未提交现场文件、发射机构是否安全”，再行动。当前最有价值的下一步不是继续改参数，而是安全烧录并验证 Phase 2D.5，用左右两侧的目标误差收敛曲线决定后续是否需要调最小启动速度、增益或滞回阈值。
