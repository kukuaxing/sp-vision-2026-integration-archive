# 2026-09-04 Phase 2D.5 实车结果

## 最终状态

- 海康相机连续取流约 65--68 FPS，曝光 2 ms、增益 10；红色装甲板蓝框识别稳定。
- 使用重新标定的内外参。最终运行配置内已固化相机矩阵、畸变、`R_camera2gimbal` 和 `t_camera2gimbal`。
- Yaw 已完成左右、中心、往返和快速移动跟随；最终观感连续顺滑，无持续振荡。
- Pitch 改为装甲板像素纵向误差闭环，误差增益 1.65，目标变化上限 80 deg/s；上下往返稳定且基本居中。
- Yaw + Pitch 联调使用横向“∞”轨迹完成中速两圈和较快一圈，用户结论为“还挺顺利”。静止后基本居中。
- 双轴运行配置为 `changed-files/configs/standard_hikrobot_phase2d5_joint_acceptance.yaml`。
- 完整双轴日志为 `joint_yaw_pitch_v1_175350.log`。

日志内共有 303 个 `mode=1` 同步样本：287 个 `disarm_reason=0`，16 个连续样本为 `disarm_reason=15`。代码映射表明 15 是可恢复的 Pitch 视觉控制丢失，不是超程硬故障；该段云台保持稳定并在目标恢复后继续跟随。未出现 Pitch 实际超程硬锁止（reason 14）或 Yaw 硬故障。此行为在后续开火阶段必须作为“丢目标立即禁射”的验收点。

## 下位机与安全边界

- 当前已烧录固件 bin SHA-256：`7708D30EE510AB4807379175C4314B1D413BD3F96CB01A3F93A92B07937A1F21`。
- Pitch：进入 AUTO 时目标限幅 ±15 deg，实际偏转超过 ±18 deg 硬锁止，目标斜坡 80 deg/s，DM 位置软限位 [-0.25, 0.40] rad。
- Yaw：进入 AUTO 时目标限幅 ±25 deg，实际偏转超过 ±28 deg 硬锁止。
- 串口：`/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`，460800 baud。
- 本轮所有自瞄验证均保持 `auto_fire: false`、`xuc_allow_shoot: false`；底盘、摩擦轮和拨弹未由视觉链路启用。
- 80 deg/s 烧录后的接收探测为 803 个有效包/8 s、字段错误 0；安全心跳为 500 个有效包/5 s、字段错误 0。

## 关键控制实现

- Yaw 使用当前装甲测量角的 30 ms 一阶滤波，最大连续变化 120 deg/s，短丢帧保持 180 ms，禁用未经验证的预测超前。
- Pitch 使用 `imu_pitch + 1.65 * atan2(armor_center_y - image_center_y, fy)` 生成绝对角目标，同样使用 30 ms 滤波与 180 ms 短丢帧保持。
- 下位机复用原有云台执行链路；视觉只提供目标角。模式按遥控器档位直接判定，离开 AUTO 立即解除自瞄接管。

## 下一阶段

开火测试尚未开始。不得直接把最终双轴配置里的两个禁射开关一起打开。应先在真正安全档确认枪口方向、弹丸与靶区、人员隔离、摩擦轮/拨弹手动逻辑和裁判系统反馈，再分别验证摩擦轮预热、单发拨弹和视觉触发；每一步失败都回真正安全档。
