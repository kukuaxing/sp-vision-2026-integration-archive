# 工作区约定

- 唯一正式工程是本仓库；小电脑路径固定为 `/home/rm/sp_vision`。
- 直接修改现有文件。禁止创建 final、copy、backup、日期或参数后缀的工程、源码、配置副本。
- 只维护当前最终版；旧提交和旧标签已按用户要求清除，不恢复历史备份。以后只在现有目录修改，使用 Git status/diff 核对；按用户要求提交/推送，禁止自动创建归档分支、标签或副本。
- 正式配置只有 `configs/standard.yaml`；标定工具配置只有 `configs/calibration.yaml`。运行 YAML 是启动器生成的缓存，不是第二份配置源。
- 编译只使用 `build/`，日志只使用 `logs/`，原始采集数据只使用 `data/`。临时文件在系统临时目录，用完删除。
- 固件按真实硬件区分 `firmware/infantry` 和 `firmware/sentry`，每种硬件只保留一份当前源码。禁止互相覆盖或混刷。
- 用户自行启动现场测试。整理、编译和离线检查不应启动自瞄、电机或发射程序。
- 改目录或文件名必须同步更新启动器、桌面入口、服务、文档和构建路径。
- 网络优先普通 HTTP/HTTPS；现场 SSH 使用用户已授权的连接，不改用插件、隧道或浏览器。

- 仓库只跟踪源码、配置、构建脚本、文档及依赖清单。禁止提交固件镜像、可执行文件、SDK动态库、模型权重、日志或录像；提交前运行 `python3 scripts/check_source_tree.py`。
