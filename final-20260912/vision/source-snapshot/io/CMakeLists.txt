cmake_minimum_required(VERSION 3.16)

find_package(yaml-cpp REQUIRED)

add_subdirectory(serial)
# 创建目标 io
add_library(io STATIC 
    daheng.cpp
    hikrobot/hikrobot.cpp    
    mindvision/mindvision.cpp  
    usbcamera/usbcamera.cpp  
    camera.cpp
    cboard.cpp
    dm_imu/dm_imu.cpp
    gimbal/gimbal.cpp
    xuc.cpp
)
# target_include_directories(io PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})

# daheng
target_include_directories(io PUBLIC driver)

# hikrobot
target_include_directories(io PUBLIC hikrobot/include)
if(CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64")
  target_link_directories(io PUBLIC hikrobot/lib/amd64)
elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64")
  target_link_directories(io PUBLIC hikrobot/lib/arm64)
else()
  message(FATAL_ERROR "Unsupported architecture: ${CMAKE_HOST_SYSTEM_PROCESSOR}!")
endif()

# mindvision
target_include_directories(io PUBLIC mindvision/include)
if(CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64")
  target_link_directories(io PUBLIC mindvision/lib/amd64)
elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64")
  target_link_directories(io PUBLIC mindvision/lib/arm64)
else()
  message(FATAL_ERROR "Unsupported architecture: ${CMAKE_HOST_SYSTEM_PROCESSOR}!")
endif()

target_link_libraries(io  tools rm MvCameraControl MVSDK usb-1.0 yaml-cpp serial)

# 大恒相机驱动需要 Galaxy SDK (libgxiapi)。未安装时跳过，保证海康/迈德威视用户也能编译。
find_library(GXIAPI_LIBRARY gxiapi)
if(GXIAPI_LIBRARY)
  target_link_libraries(io gxiapi)
  target_compile_definitions(io PRIVATE HAVE_GXIAPI)
else()
  message(WARNING "Daheng Galaxy SDK (libgxiapi) not found. Daheng camera driver will be disabled.")
endif()

################## 检查 ROS 环境 ##################
find_package(ament_cmake QUIET)
find_package(rclcpp QUIET)
find_package(std_msgs QUIET)
find_package(rosidl_typesupport_cpp QUIET)
find_package(tf2_ros QUIET)
find_package(geometry_msgs QUIET)
# find_package(sp_msgs QUIET)

# 🆕 TF2 支持（用于 CBoard 实时发布 TF）
if(ament_cmake_FOUND AND rclcpp_FOUND AND tf2_ros_FOUND AND geometry_msgs_FOUND)
    message(STATUS "ROS2 TF2 support found, enabling real-time TF publishing in CBoard.")
    target_compile_definitions(io PRIVATE AMENT_CMAKE_FOUND)
    ament_target_dependencies(io rclcpp tf2_ros geometry_msgs)
endif()

# 如果找不到 ROS 环境，则跳过 ROS 相关的部分
if(NOT ament_cmake_FOUND OR NOT rclcpp_FOUND OR NOT std_msgs_FOUND OR NOT rosidl_typesupport_cpp_FOUND OR NOT sp_msgs_FOUND)
    message(WARNING "ROS2 not found, skipping ROS2 specific code.")
else()
    message(STATUS "ROS2 environment found, compiling ROS2-related code.")

    # 添加 ROS2 相关的文件
    target_sources(io PRIVATE ros2/publish2nav.cpp ros2/ros2.cpp ros2/subscribe2nav.cpp)

    # 设置 ROS2 依赖
    include_directories(${std_msgs_INCLUDE_DIRS})
    ament_target_dependencies(io rclcpp std_msgs sp_msgs)
endif()
