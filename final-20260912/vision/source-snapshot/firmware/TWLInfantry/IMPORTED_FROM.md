# TWLInfantry 下位机源码导入说明

- 导入日期：2026-09-01
- 原始仓库：SkyCalm/Tracked-Wheel-Leg-Hybrid-Infantry
- 原始基线：`8bdf1bb`
- 实车代码快照：`3a41820`
- XUC 协议安全修复：`bcd415a`
- 上位机仓库目标目录：`firmware/TWLInfantry`

## 本次协议修复

- 统一上下位机自瞄角度协议为弧度。
- yaw 进入下位机位置环前转换为角度。
- 修复 `GetImuPitch()` 错误返回 yaw。
- pitch 使用实际反馈位置生成绝对目标，避免逐周期累加。
- 允许零 yaw、零 pitch 作为合法姿态。
- 增加合法数据包状态与 200 ms 通信超时保护。
- 右键释放后立即退出自瞄并保持当前云台位置。
- `control=false`、非法 IMU 或失联时清除开火请求。
- 增加 22 字节命令包和 12 字节反馈包的编译期长度检查。

## 已完成的离线验证

- 命令包长度：22 字节，通过。
- 反馈包长度：12 字节，通过。
- CRC16 标准向量：`0x6f91`，通过。
- 损坏数据包拒收，通过。
- 上下位机同一命令包 CRC：`0x7ed8`，交叉验证通过。
- yaw 跨越正负 180° 最短路径，通过。
- pitch 命令不重复累加，通过。
- 未收到合法包、右键释放及 200 ms 超时保护，通过。

## 尚未完成

- VisualGDB/STM32 工具链完整编译。
- 烧录到主控板。
- 电机断使能条件下串口收发验证。
- 低功率云台方向验证。
- 实车自瞄闭环与开火验证。
