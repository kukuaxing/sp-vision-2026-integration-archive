# 本机运行资源

本目录不提交模型权重、导出的模型图、图片或视频。源码仓库只跟踪这份说明。
当前正式配置使用 `yolo26.xml`、`yolo26.bin`，分类器资源为 `tiny_resnet.onnx`。
资源路径、体积及 SHA-256 见 `docs/artifacts.json`。现有小电脑资源仍在原路径，未从磁盘删除。
新克隆需要先准备资源，方法见 [依赖说明](../docs/dependencies.md)。
